# 📊 Resumo da Arquitetura Modular

## 🎯 Fluxo de Configuração

```
┌─────────────────────────────────────────────────────────────────┐
│                    INÍCIO DO CODESPACE                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              devcontainer.json carrega .env                     │
│  • Define build args (INSTALL_XDEBUG, INSTALL_NODEJS, etc)      │
│  • Injeta variáveis de ambiente no container                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Dockerfile build                               │
│  • Instala pacotes base (Apache, PHP, MariaDB, phpMyAdmin)      │
│  • Instala ferramentas opcionais (Xdebug, Node.js)              │
│  • Copia scripts modulares para /usr/local/bin/                 │
│  • Torna scripts executáveis                                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              postCreateCommand: init.sh                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│configure-    │ │configure-    │ │configure-    │
│mysql.sh      │ │php.sh        │ │apache.sh     │
│              │ │              │ │              │
│• Inicia      │ │• Atualiza    │ │• Define      │
│  MySQL       │ │  php.ini     │ │  DocumentRoot│
│• Cria banco  │ │• Limites     │ │• VirtualHost │
│• Cria usuário│ │• Timezone    │ │• Módulos     │
│• Permissões  │ │• Extensões   │ │• Reinicia    │
└──────────────┘ └──────────────┘ └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
                         ▼
              ┌──────────────────┐
              │configure-        │
              │phpmyadmin.sh     │
              │                  │
              │• Gera config.php │
              │• Blowfish secret │
              │• Apache conf     │
              │• Permissões      │
              └────────┬─────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│              🎉 AMBIENTE PRONTO!                                │
│                                                                 │
│  ✓ Apache rodando em http://localhost:80                       │
│  ✓ MySQL/MariaDB pronto em localhost:3306                      │
│  ✓ phpMyAdmin em http://localhost:80/phpmyadmin                │
│  ✓ DocumentRoot configurado                                    │
│  ✓ Banco de dados e usuário criados                            │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Reload (sem rebuild)

```
┌─────────────────────────────────────────────────────────────────┐
│        Usuário edita .devcontainer/.env                         │
│        Exemplo: APACHE_DOCUMENT_ROOT=www/public                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│     bash .devcontainer/reload-services.sh                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              1. Carrega novas variáveis do .env                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│Reconfigura   │ │Reconfigura   │ │Reconfigura   │
│PHP           │ │Apache        │ │phpMyAdmin    │
└──────────────┘ └──────────────┘ └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              2. Reinicia serviços                               │
│                 • Apache                                        │
│                 • MySQL/MariaDB                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              3. Verifica status                                 │
│                 • Apache running?                               │
│                 • MySQL responding?                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              ✨ MUDANÇAS APLICADAS!                             │
└─────────────────────────────────────────────────────────────────┘
```

## 🧩 Modularidade

Cada script é **independente** e pode ser chamado individualmente:

```bash
# Apenas MySQL
configure-mysql.sh

# Apenas Apache  
configure-apache.sh /workspaces/seu-repo

# Apenas PHP
configure-php.sh

# Apenas phpMyAdmin
configure-phpmyadmin.sh

# Todos de uma vez (usado pelo init.sh)
configure-mysql.sh && \
configure-php.sh && \
configure-apache.sh /workspaces/seu-repo && \
configure-phpmyadmin.sh
```

## 📝 Variáveis de Ambiente

```
.env (arquivo do usuário)
    │
    ├─► devcontainer.json (lê via ${localEnv:VAR})
    │       │
    │       ├─► build.args (ARG no Dockerfile)
    │       └─► remoteEnv (ENV no container)
    │
    └─► init.sh (carrega via source)
            │
            └─► configure-*.sh (herda variáveis)
```

## 🎨 Logs Coloridos

Os scripts usam cores para facilitar a leitura:

- 🟢 **Verde** - Informações e sucesso
- 🟡 **Amarelo** - Avisos (não críticos)
- 🔴 **Vermelho** - Erros
- 🔵 **Azul** - Seções/títulos

## 🔐 Segurança

```
.env
└─► NÃO commitado (via .gitignore)

.env.example  
└─► Commitado (template sem senhas)

Senhas no .env
└─► Apenas para desenvolvimento local
    ⚠️ NUNCA use senhas reais em produção
```

## 📊 Hierarquia de Configuração

```
1. Valores padrão (hardcoded nos scripts)
   ↓
2. Variáveis ENV do Dockerfile
   ↓
3. Variáveis remoteEnv do devcontainer.json
   ↓
4. Arquivo .env (maior prioridade)
```

## 🚀 Casos de Uso

### Desenvolvimento Local
```env
APP_DEBUG=true
PHP_DISPLAY_ERRORS=On
PHPMYADMIN_ALLOW_NO_PASSWORD=false
```

### Testes
```env
APP_DEBUG=true
MYSQL_DATABASE=test_db
PHP_MEMORY_LIMIT=512M
```

### Demonstração
```env
APP_DEBUG=false
PHP_DISPLAY_ERRORS=Off
APACHE_INDEXES=false
```

## 🛠️ Extensibilidade

Para adicionar novos módulos:

1. Crie `configure-novoservico.sh`
2. Adicione à `init.sh`:
   ```bash
   log_section "CONFIGURANDO NOVO SERVIÇO"
   if command -v configure-novoservico.sh >/dev/null 2>&1; then
       configure-novoservico.sh
   fi
   ```
3. Adicione ao `Dockerfile`:
   ```dockerfile
   COPY configure-novoservico.sh /usr/local/bin/
   RUN chmod +x /usr/local/bin/configure-novoservico.sh
   ```
4. Adicione variáveis ao `.env.example`

## 📈 Benefícios da Arquitetura

✅ **Manutenibilidade** - Cada módulo tem uma responsabilidade única  
✅ **Testabilidade** - Scripts podem ser testados individualmente  
✅ **Reutilização** - Módulos podem ser usados em outros projetos  
✅ **Debugging** - Fácil identificar onde está o problema  
✅ **Documentação** - Cada script é auto-documentado  
✅ **Flexibilidade** - Usuário controla tudo via .env  
✅ **Performance** - Reload sem rebuild economiza tempo  

---

**Esta arquitetura permite controle total e preciso do ambiente! 🎯**
